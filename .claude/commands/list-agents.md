Read the `AGENT.md` file in the current working directory to get:
- **Broker URL** (e.g. `http://localhost:9800`)

Fetch and display all registered agents:

```
GET {broker_url}/agents
```

Show a clear list with: name, address, role, and description for each agent.
